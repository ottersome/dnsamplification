# Ideas for improving the bottle necking

- Maybe flood TCP tables somehow, with random addresses that wont 
    return a RST packet. Izz
    - ACtually this might not work because this uses TCP and the project
    demands UDP
